package Commande;


import java.util.List;

public interface Generable {

}

/*
implémenter une liste de liste en public et l'update lors du constructeur de la planche et/ou du client
puis l'envoyer en argument à generable pour pouvoir ensuite écrire le fichier
list(list(string)) pour les clients et les fournisseurs

fournisseurList = List(id_f, List ListPlanche);

ListPlanche = List(id, nombre, jour, mois, annee, prix, longueur, largeur);
*/